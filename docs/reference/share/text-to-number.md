# Text to number

Convert numbers from text type to number
```sig
share.textToNumber("15");
```

## Parameters

* `text` is a [String](/types/string). Number in text type, that should be converted

## Examples:

To convert "15" into number type:

```blocks
let temperature = share.textToNumber("15");
```
